{% extends 'base.html.twig' %}

{% block body %}
  <div class="jumbotron jumbotron-fluid bg-success mb-5 pt-1">
    <div class="container d-flex justify-content-between align-items-start">
      <div>
        <h1 class="display-4">Whatson Web</h1>
      </div>
      <div>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
          <a class="btn btn-warning my-4 mx-5" href="{{ path('app_logout') }}">Déconnexion</a>
        {% endif %}
      </div>
    </div>
  </div>
  {% block main %}{% endblock %}
  <div class="jumbotron jumbotron-fluid mb-0 mt-5 bg-success">
    <div class="container">
      <p class="lead">Réalisé par Cassandra</p>
    </div>
  </div>
{% endblock %}