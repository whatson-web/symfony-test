<div class="col-md-4 mx-auto">
<form method="post" action="{{ path('appointment_delete', {'id': appointment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input class="form-control" type="hidden" name="_method" value="DELETE">
    <input class="form-control" type="hidden" name="_token" value="{{ csrf_token('delete' ~ appointment.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
</div>