{% extends 'base.html.twig' %}

{% block title %}Editer rendez-vous{% endblock %}

{% block body %}
    <h1>Editer un rendez-vous</h1>

    {{ include('appointment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('appointment_index') }}">Retour à la liste</a>

    {{ include('appointment/_delete_form.html.twig') }}
        <div class="RetourAuMenu">
            <a href="/">Retour au menu</a>
        </div>
{% endblock %}
