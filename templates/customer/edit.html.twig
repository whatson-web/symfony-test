{% extends 'base.html.twig' %}

{% block title %}Editer un client{% endblock %}

{% block body %}
    <h1>Editer le client</h1>

    {{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('customer_index') }}">Retour à la liste</a>

    {{ include('customer/_delete_form.html.twig') }}
        <div class="RetourAuMenu">
            <a href="/">Retour au menu</a>
        </div>
{% endblock %}
